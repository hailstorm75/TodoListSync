import {AlertColor} from "@mui/material/Alert";

const MicrosoftTodo = ({ addNotification }: {addNotification: (message: string, severity: AlertColor) => void}) => {
  return (
    <>
    </>
  );
}

export default MicrosoftTodo;